import styled from 'styled-components';
import { px2vw } from '../../Styles/global';

export const Content = styled.div`
  display: flex;
  z-index: 1;
  width: ${px2vw(239)};
  height: ${px2vw(360)};
`;
